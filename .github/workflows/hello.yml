name: Hello World Training Workflow

on:
  workflow_dispatch:

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Greet the User
        run: echo "Hello World!"

      # List all files before checkout
      - run: ls -l

      # Checkout the repository
      - uses: actions/checkout@v5

      # List all files after checkout
      - run: ls -l
